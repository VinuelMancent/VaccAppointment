using System;
using System.IO;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using VaccAppointment;

namespace Testing
{
    [TestClass]
    public class TestDay
    {
        /*
        [TestMethod]
        public void TestDaySerialisation()
        {
            Day day = new Day(DateTime.Today);
            day.AddAppointment(new Appointment());
            day.AddAppointment(new Appointment());
            day.SerializeFio();
            Assert.IsTrue(File.Exists("storage.txt"));
            //File.Delete("storage.txt");
        }*/
    }
}